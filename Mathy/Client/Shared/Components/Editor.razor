@inject JShelper JS

<div id="div1">

</div>

@code {
    [Parameter]
    public string Content { get; set; }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await JS.ClearContent("div1");
        await JS.InitEditor(Content);
    }
}
